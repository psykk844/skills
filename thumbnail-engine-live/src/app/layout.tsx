import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nano Banana Pro v2.0 - AI YouTube Thumbnail Generator",
  description: "AI-powered YouTube thumbnail generator with seamless face swapping. Powered by Google Gemini 2.0 Flash multimodal image generation.",
  keywords: ["youtube thumbnail", "face swap", "ai image generation", "gemini", "nano banana pro"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
